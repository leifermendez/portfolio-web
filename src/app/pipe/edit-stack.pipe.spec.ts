import { EditStackPipe } from './edit-stack.pipe';

describe('EditStackPipe', () => {
  it('create an instance', () => {
    const pipe = new EditStackPipe();
    expect(pipe).toBeTruthy();
  });
});
