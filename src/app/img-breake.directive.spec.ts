import { ImagBrakeDirective } from './imag-brake.directive';

describe('ImgBreakeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagBrakeDirective();
    expect(directive).toBeTruthy();
  });
});
