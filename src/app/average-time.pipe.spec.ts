import { AverageTimePipe } from './average-time.pipe';

describe('AverageTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageTimePipe();
    expect(pipe).toBeTruthy();
  });
});
